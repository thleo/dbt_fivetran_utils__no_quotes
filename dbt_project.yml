name: 'fivetran_utils__no_quotes'
version: '0.1.0'
config-version: 2

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]


vars:
  fivetran_utils__no_quotes:
    dbt_utils_dispatch_list:
      - fivetran_utils__no_quotes 